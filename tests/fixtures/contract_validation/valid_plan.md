# Contract Validation Fixture

This intentionally minimal plan verifies the legacy plan-only CI entry point.

## Verification

Repository contracts are validated through the canonical external-contract registry.

name: PR-190 corrective pytest capture

on:
  push:
    branches: [pr-190-cleanup-temporary-diagnostics]
    paths:
      - .github/workflows/pr190-corrective-pytest.yml

permissions:
  contents: write

jobs:
  capture-pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: pr-190-cleanup-temporary-diagnostics
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: python -m pip install --requirement requirements.txt
      - run: python -m pip install --requirement requirements-dev.txt
      - run: python -m pip install --no-deps .
      - name: Capture pytest
        shell: bash
        run: |
          set +e
          python -m pytest -q > pr190-corrective-pytest.txt 2>&1
          status=$?
          echo "exit_status=$status" >> pr190-corrective-pytest.txt
          exit 0
      - name: Commit diagnostics temporarily
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pr190-corrective-pytest.txt
          git commit -m "PR-190: capture corrective pytest output"
          git push origin HEAD:pr-190-cleanup-temporary-diagnostics
